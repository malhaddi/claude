# Copy to .env.local and adjust. Never commit .env.local.
#
# Public base URL of the site, used for canonical URLs and Open Graph
# metadata. Set it to the production domain on Vercel.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Later milestones (do NOT set yet — the code does not read them):
# - Supabase: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
# - Stripe: STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY
# - AI provider: e.g. ANTHROPIC_API_KEY
